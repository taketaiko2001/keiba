# keiba - 競馬仮想馬券シミュレーション

このリポジトリの目的と運用ルールの要約です。詳細は各ファイルを参照してください。

## 最重要の制約
- **実際の馬券購入・送金は一切行わない。** すべて記録上の仮想シミュレーション（購入者本人は馬券を買わない）。
- 予算: 1レースあたり仮想投資上限5,000円。原資100万円→目標120万円。
- 対象範囲(2026-09-01〜): **中央競馬(JRA)のみ・土日のみ**。トークン利用量節約のため地方競馬(NAR)の予想・購入は一旦停止中。

## ファイル構成
- `README.md` — プロジェクト概要、`tools/`配下のデータ取得スクリプトの説明
- `競馬運用記録.md` — 運用ルール、収支サマリー、レースごとの買い目・結果・反省欄（詳細な履歴はここに蓄積）
- `.claude/skills/keiba-hyoka-houshin/SKILL.md` — 買い目の評価・購入方針の詳細手順（S〜C格付け、オッズ妙味判定、トリガミ回避等）。**レース評価・買い目確定を行う際は必ずこのスキルを読むこと。**

## 毎回やること（要約、詳細はskill参照）
1. 現在時刻を確認。`pip install -q -r tools/requirements.txt`を実行しておく。
2. 記録.mdで「結果：未確定」のレースがあれば結果を取得・反映する。
3. 発走10〜70分後の範囲にあるJRAレースを確認する（G3以上の重賞は最優先）。
4. 候補レースの出走馬を、上記skillの評価プロセス（S〜C格付け→オッズ妙味判定）で評価する。
5. 妙味のある買い目を組み立て購入する（データ信頼性不足時のみ見送り可）。購入前に必ずトリガミ回避チェックを行う。
6. 選定理由を含めて記録.mdに新しいレースとして追記する。
7. 変更があれば git add / commit / push（日本語コミット）。

## 厳守事項
- 実際の馬券購入・送金は絶対に行わない。記録ファイルへの追記のみ。
- 同じレースへの二重購入禁止、5,000円/レースの予算上限、発走時刻を過ぎたレースには購入しない。
- JRA開催日（土日）は最低2レース購入（データ信頼性不足の場合を除く）。
- G3以上の重賞は必須購入（データ信頼性不足の場合を除く）。
- 人気（単勝オッズの順位）だけを根拠に軸馬を機械的に決めない。
- 組み立てた買い目は、どの券が的中しても払戻総額が購入総額を下回らないか（トリガミ回避）を購入前に必ず検算する。複勝を一律の保険として使わない。
- 回収率は数レース単位の勝敗で一喜一憂せず、あくまで累積回収率で判断する。

## データ取得の優先順位
1. `tools/*.py`（`pip install -q -r tools/requirements.txt`後、Bashで直接実行）— 生HTMLを直接パースするため誤読なし。この環境(keiba-env2)はNetwork accessがCustomで race.netkeiba.com / nar.netkeiba.com / db.netkeiba.com / www.jra.go.jp / www.oddspark.com への直接アクセスを許可済み。**使えるなら常にこれを主データ源にする。**
2. `tools/*.py`が接続エラー(403・Connection refused・timeoutなど)で失敗する場合のみWebFetchにフォールバック。数値を明示的に列挙させ、人気とオッズの整合性を自分で検算する。不自然な値なら信用しない。
3. WebFetchも`EGRESS_BLOCKED`等で使用不能ならWebSearchのみで判断可能な範囲に留め、安全対策を通過できないなら見送る。

## RemoteTriggerルーティン
- ID: `trig_0158hKaa54vBiqB6Wnz371ny`、名前「競馬仮想馬券シミュレーション」
- cron: `7 0-12 * * 0,6`（JST土日9-21時、毎正時07分）
- 環境: keiba-env2（Network accessがCustomで上記ドメインへ直接アクセス可）
- ルーティンの`update`は`job_config.ccr`を毎回フルセット（environment_id・session_context・events全文）で送ること。部分更新すると`prompt`等が消える。

## 既知の落とし穴
- WebFetchが数十時間規模で全ドメイン`EGRESS_BLOCKED`になることがある。1〜2回は静かに見送り、3回以上連続なら記録・運用者へ報告する。
- `tools/netkeiba_fetch.py`の`body_weight_kg`欄が単勝オッズと酷似した値を返す列ずれ不具合が時々発生する。発生した馬はその項目の評価から除外する。
- `~/.claude/skills/`（ユーザースコープ）のスキルはこのクラウドルーティンからは見えない。ルーティンに使わせたいスキルは必ずこのリポジトリの`.claude/skills/`にコミットすること。
